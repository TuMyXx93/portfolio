# Engram Status Command

Alias: /engram-status

Action:

1. Run `engram stats` to show memory statistics
2. Run `engram projects list` to show project hygiene
3. Report memory health and project hygiene status
4. If cloud is configured, show sync status via `engram sync --cloud --status --project <name>`
5. Flag any project name drift or similar-project warnings
